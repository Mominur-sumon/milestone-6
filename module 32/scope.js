
function add (a, b) {
  const sum = a + b;
  return sum;
  
}

const result = add(1, 2);
console.log(result);
// console.log(a,b); // this will throw an error because a and b are not defined in the global scope
